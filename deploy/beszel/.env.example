# Beszel hub
BESZEL_PORT=8090
BESZEL_APP_URL=http://127.0.0.1:8090

# Agent (only when using --profile agent)
# Create a system in the hub UI → copy token + public key into these vars.
BESZEL_HUB_URL=http://127.0.0.1:8090
BESZEL_AGENT_TOKEN=
BESZEL_AGENT_KEY=
